# 大所重跑指南 v2（1 家/agent，2026-08-28）

你負責 **1 家大所**（11–211 人）。這些所站上原本是 4 月的舊格式分析（無七節、無營收推估），你要用新格式**整份重寫**。目標 4–6 次工具往返完成。

## 材料（已隨本檔一起 cat 給你，不要再讀第二次、不要再查 DB）
- `FORMAT.md` — ai_analysis 產出格式（七節＋30 秒速讀＋可讀性鐵則），**照它寫**
- `MARKET.md` — 行情假設表（78 項）。**第六節營收推估一律用這張表的區間，不要自己假設價格**
- `REVENUE_RULES.md` — 訴訟收費公式 v2（標的加費／審級／身分係數與掛名排除），第六節照它算
- `../slim/<所名>.txt` — 該所 DB 資料（**大所精簡版**，見下方讀法）
- `../sites/<所名>.txt` — 官網正文（已預抓）；開頭若是「官網無法存取」就照無官網分析

## 大所 dossier 讀法（重要）
律師名冊段開頭有 **【全所彙總】**（在籍人數、近5年案量合計、人均、案類合計、年資金字塔、同名旗標人數）。
- **全所規模、人均案量、人才結構一律引用【全所彙總】的數字**
- 底下只列「近5年案量 TOP30 ＋ 最資深 6 位」，**不要把這些人的案量加總當成全所總量**（會嚴重低估）
- 逐年案量／案由／客戶集中度也只涵蓋這些被列出的人——引用時說明是「主力律師層」

## 步驟 2：寫 `C:\projects\legal-industry-intel\scripts\_batch408\out\<所名>.md`（UTF-8，一次寫完）

```
---
practice_focus: 公司併購, 智財訴訟, 金融法遵      # 2–6 項，逗號分隔
founded_year: 1965                              # 查無寫 -
ex_judicial: 王小明｜前最高法院法官｜審判資歷 20 年   # 多筆用 " ;; "；無寫 -
leaders: 王小明｜所長｜官網團隊頁+DB名冊姓名核對      # 多筆 " ;; "；無法確認寫 -
tagline: 台灣最大所，跨國企業法律服務的代名詞        # ≤18 字
type: 企業非訟型                                 # B2C行銷型|傳統轉介型|機構標案型|企業非訟型|精品專業型|一人綜合型
notes: 官網為集團品牌站，未列個別律師案量           # 邊界案例/資料品質問題，無則留空
---
（以下接 ai_analysis 全文 markdown，**上限 6,000 字，硬性**。直接從 `## ⚡ 30 秒速讀` 開始）

**6,000 字是硬上限，不是建議值**。寫完先自己數字數，超過就精簡再寫檔，不要寫完才砍。控制長度的方法（依序）：
1. 表格只留關鍵欄位，不要把 dossier 的數字整排搬進來
2. 同名校正用「帳面 X 件 → 校正後 Y 件（原因）」一句話帶過，不做雙欄對照表
3. 每節的量條／箭頭鏈各留 1 組，不要每個小標都配一組
4. 口徑警語集中寫在第六節末尾一次，不要每節重複
七節、30 秒速讀、三側營收推估、掛名校正結論、口徑警語**都必須保留**——要砍的是敘述密度與重複表格，不是資料點。
```

大所加碼要求（比小所嚴）：
- 螢光 `==…==` 每節 2–3 處；30 秒速讀必須含「規模與案量／最大亮點／最大風險」三條
- **第六節營收推估是這次重跑的重點**（舊分析完全沒有）：訴訟側用 MARKET.md 的案由行情 × 案量推「保守～樂觀」年營收區間；非訟側用獨董席次／TIPO 商標專利件數／政府標案實數分別估；三側分開列，最後給合計區間
- 必附口徑警語：裁判書件數≠委任案數（一案多審級）；現任名冊回溯口徑；行情為假設區間非實查
- 第二節要點出**明星依賴度**（TOP1／TOP3 律師佔主力層案量比重）與案由版圖
- 第五節人員動態：用「人員異動」段的轉入／轉出，大所要看團隊級流動

## 步驟 3：上傳（一行搞定，會同時 POST DB＋寫 leaders json）
```
cd C:\projects\legal-industry-intel\scripts\_batch408\v2 && python upload_one.py <所名>
```
印 `OK …` 即成功；印 `ERR …` 就照訊息修 **out 檔**再跑一次。
**不要修改 upload_one.py 或任何共用 script**（已修好 on_conflict，能正常 upsert 覆蓋舊分析）；也不要自己寫 python 打 API。

## 步驟 4：回報一行
`<所名>｜OK｜<型態>｜<一句定位>｜leaders=<名字或無>｜<異常備註或->`

## 判斷規則（品質底線）
- **所長**：必須是 slim dossier 名冊裡真實存在的人（大所看官網「所長／執行長／managing partner／主持律師」頁）。官網只有英文品牌名時不得音譯推測中文名。無法確認寫 `-` 並在 notes 說明。
- **前司法官**：只認 dossier `ex_judicial_lawyers`（firm_name 相符）＋官網明確寫出且與名冊**姓名核對通過**者。法官助理／書記官／檢察事務官／考上未任職**不算**。大所常有前大法官／前部長，務必姓名核對。
- **查無就寫「查無」**，禁止編造。`name_ambiguous=true` 者案量是同名合併值，引用要標註。
- **WebSearch**：大所可用，**最多 2 次**，僅用於確認創所年份、所長／合夥人結構、重大案件或前司法官身分。結果必須與 dossier 名冊姓名核對。

## 禁令
不動 `public/index.html`、不 git、不跑 Ad Library、不查 DB、不 WebFetch（官網已預抓）、不重複讀已 cat 過的檔。
